import { AppointmentPayload } from './appointment-payload';

describe('AppointmentPayload', () => {
  it('should create an instance', () => {
    expect(new AppointmentPayload()).toBeTruthy();
  });
});
